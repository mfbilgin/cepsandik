env JWT_SECRET;
env INTERNAL_JWT_SECRET;
# Faz 3.11 — rate-limit override (set edilmezse prod-makul default: 15/100/60)
env RL_AUTH;
env RL_PROTECTED;
env RL_DEFAULT;

events {
    worker_connections 1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;

    lua_package_path "/usr/local/openresty/nginx/lua/?.lua;;";
    lua_shared_dict rate_limit 10m;

    # Docker internal DNS resolver (Essential for 502 fixes on container rebuild)
    # NOTE: `upstream {}` blocks resolve DNS once at start-time and cache the IP
    # forever — `resolver valid=Ns` does NOT apply to upstream blocks. Below we
    # use variable-based proxy_pass so DNS is re-resolved per-request honoring
    # the resolver TTL.
    resolver 127.0.0.11 valid=30s ipv6=off;

    server {
        listen 80;
        server_name _;

        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        location /v1/auth {
            access_by_lua_block {
                local rate_limit = require "rate_limit"
                rate_limit.check("auth")
            }
            set $user_upstream "user-service:8080";
            rewrite ^/v1/auth(.*)$ /api/v1/auth$1 break;
            proxy_pass http://$user_upstream;
        }

        location ^~ /v1/users/confirm-email-change {
            access_by_lua_block {
                local rate_limit = require "rate_limit"
                rate_limit.check("auth")
            }
            set $user_upstream "user-service:8080";
            rewrite ^/v1/users/confirm-email-change(.*)$ /api/v1/users/confirm-email-change$1 break;
            proxy_pass http://$user_upstream;
        }

        location /v1/users {
            access_by_lua_block {
                local rate_limit = require "rate_limit"
                rate_limit.check("protected")

                local jwt = require "resty.jwt"
                local cjson = require "cjson"

                local JWT_SECRET = os.getenv("JWT_SECRET") or (ngx.log(ngx.ERR, "FATAL: JWT_SECRET env tanimsiz - istek reddedildi") or ngx.exit(500))
                local INTERNAL_SECRET = os.getenv("INTERNAL_JWT_SECRET") or (ngx.log(ngx.ERR, "FATAL: INTERNAL_JWT_SECRET env tanimsiz - istek reddedildi") or ngx.exit(500))

                local auth_header = ngx.var.http_authorization
                if not auth_header then
                    ngx.status = 401
                    ngx.header["Content-Type"] = "application/json"
                    ngx.say(cjson.encode({success = false, message = "Bu islem icin giris yapmaniz gerekmektedir."}))
                    return ngx.exit(401)
                end

                local token = auth_header:match("^Bearer%s+(.+)$")
                if not token then
                    ngx.status = 401
                    ngx.header["Content-Type"] = "application/json"
                    ngx.say(cjson.encode({success = false, message = "Gecersiz kimlik bilgisi formati."}))
                    return ngx.exit(401)
                end

                local jwt_obj = jwt:verify(JWT_SECRET, token)
                if not jwt_obj.verified then
                    ngx.status = 401
                    ngx.header["Content-Type"] = "application/json"
                    ngx.say(cjson.encode({success = false, message = "Oturum dogrulanamadi, lutfen tekrar giris yapin."}))
                    return ngx.exit(401)
                end

                local user_id = jwt_obj.payload.sub or jwt_obj.payload.userId or jwt_obj.payload.id
                local roles = jwt_obj.payload.roles or {"USER"}
                local platform_role = jwt_obj.payload.platformRole or "USER"

                if not user_id then
                    ngx.status = 401
                    ngx.header["Content-Type"] = "application/json"
                    ngx.say(cjson.encode({success = false, message = "Oturum bilgisi eksik, lutfen tekrar giris yapin."}))
                    return ngx.exit(401)
                end

                local now = ngx.time()
                local internal_jwt = jwt:sign(INTERNAL_SECRET, {
                    header = {typ = "JWT", alg = "HS256"},
                    payload = {
                        uid = user_id,
                        roles = roles,
                        scope = {"community:read", "community:write"},
                        iss = "api-gateway",
                        iat = now,
                        exp = now + 30
                    }
                })

                ngx.req.set_header("X-Internal-Auth", internal_jwt)
                ngx.req.set_header("X-User-Id", tostring(user_id))
                ngx.req.set_header("X-Platform-Role", platform_role)
            }
            set $user_upstream "user-service:8080";
            rewrite ^/v1/users(.*)$ /api/v1/users$1 break;
            proxy_pass http://$user_upstream;
        }

        location /v1/communities {
            access_by_lua_block {
                local rate_limit = require "rate_limit"
                rate_limit.check("protected")

                local jwt = require "resty.jwt"
                local cjson = require "cjson"

                local JWT_SECRET = os.getenv("JWT_SECRET") or (ngx.log(ngx.ERR, "FATAL: JWT_SECRET env tanimsiz - istek reddedildi") or ngx.exit(500))
                local INTERNAL_SECRET = os.getenv("INTERNAL_JWT_SECRET") or (ngx.log(ngx.ERR, "FATAL: INTERNAL_JWT_SECRET env tanimsiz - istek reddedildi") or ngx.exit(500))

                local auth_header = ngx.var.http_authorization
                local token = auth_header and auth_header:match("^Bearer%s+(.+)$")
                if not token then
                    ngx.status = 401
                    ngx.header["Content-Type"] = "application/json"
                    ngx.say(cjson.encode({success = false, message = "Yetkisiz erisim."}))
                    return ngx.exit(401)
                end

                local jwt_obj = jwt:verify(JWT_SECRET, token)
                if not jwt_obj.verified then
                    ngx.status = 401
                    ngx.header["Content-Type"] = "application/json"
                    ngx.say(cjson.encode({success = false, message = "Yetkisiz erisim."}))
                    return ngx.exit(401)
                end

                local user_id = jwt_obj.payload.sub or jwt_obj.payload.userId or jwt_obj.payload.id
                local roles = jwt_obj.payload.roles or {"USER"}
                local platform_role = jwt_obj.payload.platformRole or "USER"
                local now = ngx.time()
                local internal_jwt = jwt:sign(INTERNAL_SECRET, {
                    header = {typ = "JWT", alg = "HS256"},
                    payload = {
                        uid = user_id,
                        roles = roles,
                        scope = {"community:read", "community:write"},
                        iss = "api-gateway",
                        iat = now,
                        exp = now + 30
                    }
                })

                ngx.req.set_header("X-Internal-Auth", internal_jwt)
                ngx.req.set_header("X-User-Id", tostring(user_id))
                ngx.req.set_header("X-Platform-Role", platform_role)
            }
            set $community_upstream "community-service:8083";
            rewrite ^/v1/communities(.*)$ /api/v1/communities$1 break;
            proxy_pass http://$community_upstream;
        }

        # Faz 1.3 — PUBLIC audit record (E2E-V bağımsız doğrulama).
        # Kullanıcı JWT'si GEREKTİRMEZ (denetçi/gözlemci anonimdir). İçerik
        # tamamen şeffaflık verisi; gizli hiçbir şey yok. Regex location
        # plain-prefix /v1/elections'tan önce eşleşir → JWT bloğunu bypass eder.
        # Internal-auth modeli korunur: gateway servis kimliğiyle internal JWT
        # imzalar (kullanıcı yok), election-service InternalJwtFilter kabul eder.
        location ~ ^/v1/elections/[0-9]+/audit-record$ {
            access_by_lua_block {
                local rate_limit = require "rate_limit"
                rate_limit.check("default")

                local jwt = require "resty.jwt"
                local INTERNAL_SECRET = os.getenv("INTERNAL_JWT_SECRET") or (ngx.log(ngx.ERR, "FATAL: INTERNAL_JWT_SECRET env tanimsiz - istek reddedildi") or ngx.exit(500))
                local now = ngx.time()
                local internal_jwt = jwt:sign(INTERNAL_SECRET, {
                    header = {typ = "JWT", alg = "HS256"},
                    payload = {
                        uid = "public-audit",
                        roles = {"PUBLIC"},
                        scope = {"election:read"},
                        iss = "api-gateway",
                        iat = now,
                        exp = now + 30
                    }
                })
                ngx.req.set_header("X-Internal-Auth", internal_jwt)
                ngx.req.set_header("X-User-Id", "public-audit")
            }
            set $election_upstream "election-service:8082";
            rewrite ^/v1/elections(.*)$ /api/v1/elections$1 break;
            proxy_pass http://$election_upstream;
        }

        location /v1/elections {
            access_by_lua_block {
                local rate_limit = require "rate_limit"
                rate_limit.check("protected")

                local jwt = require "resty.jwt"
                local cjson = require "cjson"

                local JWT_SECRET = os.getenv("JWT_SECRET") or (ngx.log(ngx.ERR, "FATAL: JWT_SECRET env tanimsiz - istek reddedildi") or ngx.exit(500))
                local INTERNAL_SECRET = os.getenv("INTERNAL_JWT_SECRET") or (ngx.log(ngx.ERR, "FATAL: INTERNAL_JWT_SECRET env tanimsiz - istek reddedildi") or ngx.exit(500))

                local auth_header = ngx.var.http_authorization
                local token = auth_header and auth_header:match("^Bearer%s+(.+)$")
                if not token then
                    ngx.status = 401
                    ngx.header["Content-Type"] = "application/json"
                    ngx.say(cjson.encode({success = false, message = "Yetkisiz erisim."}))
                    return ngx.exit(401)
                end

                local jwt_obj = jwt:verify(JWT_SECRET, token)
                if not jwt_obj.verified then
                    ngx.status = 401
                    ngx.header["Content-Type"] = "application/json"
                    ngx.say(cjson.encode({success = false, message = "Yetkisiz erisim."}))
                    return ngx.exit(401)
                end

                local user_id = jwt_obj.payload.sub or jwt_obj.payload.userId or jwt_obj.payload.id
                local roles = jwt_obj.payload.roles or {"USER"}
                local platform_role = jwt_obj.payload.platformRole or "USER"
                local now = ngx.time()
                local internal_jwt = jwt:sign(INTERNAL_SECRET, {
                    header = {typ = "JWT", alg = "HS256"},
                    payload = {
                        uid = user_id,
                        roles = roles,
                        scope = {"election:read", "election:write"},
                        iss = "api-gateway",
                        iat = now,
                        exp = now + 30
                    }
                })

                ngx.req.set_header("X-Internal-Auth", internal_jwt)
                ngx.req.set_header("X-User-Id", tostring(user_id))
                ngx.req.set_header("X-Platform-Role", platform_role)
            }
            set $election_upstream "election-service:8082";
            rewrite ^/v1/elections(.*)$ /api/v1/elections$1 break;
            proxy_pass http://$election_upstream;
        }

        # Guardian (Emanetçi) endpoint'leri — GuardianController @ /api/v1/guardians.
        # Sprint 5.A.distributed: dağıtık key ceremony bu route üzerinden çalışır.
        # elections bloğunun birebir aynısı, sadece path rewrite farklı.
        location /v1/guardians {
            access_by_lua_block {
                local rate_limit = require "rate_limit"
                rate_limit.check("protected")

                local jwt = require "resty.jwt"
                local cjson = require "cjson"

                local JWT_SECRET = os.getenv("JWT_SECRET") or (ngx.log(ngx.ERR, "FATAL: JWT_SECRET env tanimsiz - istek reddedildi") or ngx.exit(500))
                local INTERNAL_SECRET = os.getenv("INTERNAL_JWT_SECRET") or (ngx.log(ngx.ERR, "FATAL: INTERNAL_JWT_SECRET env tanimsiz - istek reddedildi") or ngx.exit(500))

                local auth_header = ngx.var.http_authorization
                local token = auth_header and auth_header:match("^Bearer%s+(.+)$")
                if not token then
                    ngx.status = 401
                    ngx.header["Content-Type"] = "application/json"
                    ngx.say(cjson.encode({success = false, message = "Yetkisiz erisim."}))
                    return ngx.exit(401)
                end

                local jwt_obj = jwt:verify(JWT_SECRET, token)
                if not jwt_obj.verified then
                    ngx.status = 401
                    ngx.header["Content-Type"] = "application/json"
                    ngx.say(cjson.encode({success = false, message = "Yetkisiz erisim."}))
                    return ngx.exit(401)
                end

                local user_id = jwt_obj.payload.sub or jwt_obj.payload.userId or jwt_obj.payload.id
                local roles = jwt_obj.payload.roles or {"USER"}
                local platform_role = jwt_obj.payload.platformRole or "USER"
                local now = ngx.time()
                local internal_jwt = jwt:sign(INTERNAL_SECRET, {
                    header = {typ = "JWT", alg = "HS256"},
                    payload = {
                        uid = user_id,
                        roles = roles,
                        scope = {"election:read", "election:write"},
                        iss = "api-gateway",
                        iat = now,
                        exp = now + 30
                    }
                })

                ngx.req.set_header("X-Internal-Auth", internal_jwt)
                ngx.req.set_header("X-User-Id", tostring(user_id))
                ngx.req.set_header("X-Platform-Role", platform_role)
            }
            set $guardian_upstream "election-service:8082";
            rewrite ^/v1/guardians(.*)$ /api/v1/guardians$1 break;
            proxy_pass http://$guardian_upstream;
        }

        # Notification inbox — NotificationController @ /api/v1/notifications.
        # GUARDIAN_DUTY event'leri DB'de persist edilir; mobile burayı çağırır.
        # Diğer route'ların birebir aynı şablonu: JWT verify → internal JWT
        # inject (scope notification:read/write) → X-User-Id header → upstream.
        location /v1/notifications {
            access_by_lua_block {
                local rate_limit = require "rate_limit"
                rate_limit.check("protected")

                local jwt = require "resty.jwt"
                local cjson = require "cjson"

                local JWT_SECRET = os.getenv("JWT_SECRET") or (ngx.log(ngx.ERR, "FATAL: JWT_SECRET env tanimsiz - istek reddedildi") or ngx.exit(500))
                local INTERNAL_SECRET = os.getenv("INTERNAL_JWT_SECRET") or (ngx.log(ngx.ERR, "FATAL: INTERNAL_JWT_SECRET env tanimsiz - istek reddedildi") or ngx.exit(500))

                local auth_header = ngx.var.http_authorization
                local token = auth_header and auth_header:match("^Bearer%s+(.+)$")
                if not token then
                    ngx.status = 401
                    ngx.header["Content-Type"] = "application/json"
                    ngx.say(cjson.encode({success = false, message = "Yetkisiz erisim."}))
                    return ngx.exit(401)
                end

                local jwt_obj = jwt:verify(JWT_SECRET, token)
                if not jwt_obj.verified then
                    ngx.status = 401
                    ngx.header["Content-Type"] = "application/json"
                    ngx.say(cjson.encode({success = false, message = "Yetkisiz erisim."}))
                    return ngx.exit(401)
                end

                local user_id = jwt_obj.payload.sub or jwt_obj.payload.userId or jwt_obj.payload.id
                local roles = jwt_obj.payload.roles or {"USER"}
                local platform_role = jwt_obj.payload.platformRole or "USER"
                local now = ngx.time()
                local internal_jwt = jwt:sign(INTERNAL_SECRET, {
                    header = {typ = "JWT", alg = "HS256"},
                    payload = {
                        uid = user_id,
                        roles = roles,
                        scope = {"notification:read", "notification:write"},
                        iss = "api-gateway",
                        iat = now,
                        exp = now + 30
                    }
                })

                ngx.req.set_header("X-Internal-Auth", internal_jwt)
                ngx.req.set_header("X-User-Id", tostring(user_id))
                ngx.req.set_header("X-Platform-Role", platform_role)
            }
            set $notif_upstream "notification-service:8084";
            rewrite ^/v1/notifications(.*)$ /api/v1/notifications$1 break;
            proxy_pass http://$notif_upstream;
        }

        location /user/actuator {
            set $user_upstream "user-service:8080";
            rewrite ^/user/actuator/(.*)$ /actuator/$1 break;
            rewrite ^/user/actuator$ /actuator break;
            proxy_pass http://$user_upstream;
        }

        location /community/actuator {
            set $community_upstream "community-service:8083";
            rewrite ^/community/actuator/(.*)$ /actuator/$1 break;
            rewrite ^/community/actuator$ /actuator break;
            proxy_pass http://$community_upstream;
        }

        location /election/actuator {
            set $election_upstream "election-service:8082";
            rewrite ^/election/actuator/(.*)$ /actuator/$1 break;
            rewrite ^/election/actuator$ /actuator break;
            proxy_pass http://$election_upstream;
        }
    }
}
